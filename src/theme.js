import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: { main: "#b15d5dff" },
    secondary: { main: "#4CAF50" },
    background: { default: "#f8f9fb" }
  },
  typography: {
    fontFamily: "Outfit, sans-serif",
  },
});